# คู่มือการใช้งาน Classroom Finance 5.0

## 🚀 เริ่มต้นใช้งาน

### 1. ติดตั้ง Dependencies
```bash
cd classroom-finance-5
npm install
```

### 2. ตั้งค่า Environment Variables
```bash
# สร้างไฟล์ .env.local (มีไฟล์ตัวอย่างอยู่แล้ว)
cp .env.local.example .env.local
```

### 3. รันโปรแกรม
```bash
npm run dev
```
เปิดเบราว์เซอร์ที่ http://localhost:3000

---

## 📖 คู่มือการใช้งานแต่ละหน้า

### 🏠 Dashboard (หน้าหลัก)
แสดงภาพรวมการเงินทั้งหมด

**ฟีเจอร์:**
- การ์ดยอดคงเหลือ (รายรับทั้งหมด - รายจ่ายทั้งหมด)
- การ์ดรายรับจากธุรกรรม (ไม่รวมการเก็บนักเรียน)
- การ์ดรายจ่าย
- การ์ดรายรับจากการเก็บนักเรียน (แบ่งตาม ธนาคาร/เงินสด/TrueMoney)
- จำนวนนักเรียนที่ชำระแล้ว/ค้างชำระ (เลือกกำหนดการได้)
- แผนภูมิวงกลมแสดงรายรับรายจ่ายตามหมวดหมู่ (เลือกเดือนได้)

**วิธีใช้:**
1. ดูยอดคงเหลือจากการ์ดด้านบน
2. เลือกกำหนดการเพื่อดูสถานะการชำระของนักเรียน
3. เลือกเดือนในแผนภูมิเพื่อดูสัดส่วนรายรับรายจ่าย

---

### 💳 Transactions (บันทึกรายการ)
จัดการรายรับรายจ่ายทั้งหมด

**ฟีเจอร์:**
- กรองรายการ (ธุรกรรม/กำหนดการ, รายรับ/รายจ่าย, ประเภทการชำระ)
- ค้นหารายการ
- ตารางแสดงรายละเอียดทุกธุรกรรม
- แสดงชื่อผู้ชำระ (เหรัญญิก หรือ ชื่อนักเรียน)

**วิธีใช้:**
1. เลือกตัวกรองที่ต้องการ
2. พิมพ์คำค้นหาในช่องค้นหา
3. กดปุ่ม "เพิ่ม" เพื่อเพิ่มรายการใหม่ (ยังไม่ทำงาน - รอ Phase 2)

**ตารางแสดง:**
- ชื่อรายการ
- ผู้ชำระ (เหรัญญิก = ธุรกรรมปกติ, ชื่อนักเรียน = จากกำหนดการ)
- จำนวนเงิน (สีเขียว = รายรับ, สีแดง = รายจ่าย)
- ประเภทการชำระ (bank/cash/truemoney)
- หมวดหมู่ (เฉพาะธุรกรรมปกติ)
- วันที่และเวลา

---

### 📅 Schedule (กำหนดการ)
จัดการกำหนดการเก็บเงิน

**ฟีเจอร์:**
- เพิ่มกำหนดการใหม่
- การ์ดแสดงกำหนดการ (เลื่อนซ้าย-ขวา ดูได้สูงสุด 5 การ์ด)
- แสดงจำนวนที่ต้องเก็บต่อรายการ
- แสดงยอดเก็บได้/ยอดที่ต้องเก็บทั้งหมด
- นับถอยหลังวันที่เหลือ
- ปฏิทินแสดงกำหนดการ (ยังไม่ทำ)

**วิธีใช้:**
1. กดปุ่ม "เพิ่มกำหนดการ" (ยังไม่ทำงาน - รอ Phase 2)
2. ใช้ลูกศรซ้าย-ขวาเลื่อนดูการ์ดกำหนดการ
3. คลิกที่การ์ดเพื่อดูรายละเอียด (ยังไม่ทำงาน - รอ Phase 2)

**ข้อมูลในการ์ด:**
- ชื่อกำหนดการ
- จำนวนเงินต่อรายการ
- ยอดเก็บได้ / ยอดที่ต้องเก็บทั้งหมด
- วันที่เหลือ (นับถอยหลัง)

---

### 👥 Students (นักเรียน)
จัดการข้อมูลนักเรียน

**ฟีเจอร์:**
- การ์ดเพิ่มนักเรียน (+ สีเทา)
- การ์ดนักเรียนแสดงเรียงตามเลขที่
- แสดงรูปโปรไฟล์ (ถ้าไม่มีจะแสดงไอคอนเริ่มต้น)
- แสดงเลขที่, คำนำหน้า, ชื่อ, ชื่อเล่น

**วิธีใช้:**
1. กดการ์ด "+" เพื่อเพิ่มนักเรียน (ยังไม่ทำงาน - รอ Phase 2)
2. คลิกที่การ์ดนักเรียนเพื่อดูรายละเอียด (ยังไม่ทำงาน - รอ Phase 2)

**ข้อมูลในการ์ด:**
- รูปโปรไฟล์
- เลขที่
- คำนำหน้า + ชื่อ
- ชื่อเล่น

---

## 🎯 ระบบการคำนวณ

### ยอดคงเหลือ (Balance)
```
ยอดคงเหลือ = รายรับจากธุรกรรม + รายรับจากการเก็บนักเรียน - รายจ่ายทั้งหมด
```

### รายรับจากธุรกรรม
- เฉพาะรายการที่เป็น `source: "transaction"` และ `kind: "income"`
- **ไม่รวม**การเก็บเงินจากนักเรียน

### รายรับจากการเก็บนักเรียน
- เฉพาะรายการที่เป็น `source: "schedule"`
- แบ่งตาม `method: "bank" | "cash" | "truemoney"`

### รายจ่าย
- เฉพาะรายการที่เป็น `source: "transaction"` และ `kind: "expense"`

### สถานะการชำระ
- **ชำระแล้ว**: จำนวนนักเรียนที่มีธุรกรรมในกำหนดการนั้นๆ
- **ค้างชำระ**: จำนวนนักเรียนทั้งหมดในกำหนดการ - จำนวนที่ชำระแล้ว

---

## 📊 ข้อมูลตัวอย่าง (Mock Data)

ขณะนี้ระบบใช้ข้อมูลปลอมเพื่อทดสอบ:
- นักเรียน 30 คน
- กำหนดการ 2 รายการ
  1. เงินค่าหนังสือรุ่น (200 บาท, 30 คน, 14 วัน)
  2. เงินทัศนศึกษา (500 บาท, 20 คน, 30 วัน)
- ธุรกรรม 10 รายการ (รายรับ/รายจ่ายสลับ)
- การชำระจากนักเรียน 50% ของแต่ละกำหนดการ

**หมายเหตุ:** ข้อมูลเหล่านี้จะถูกแทนที่ด้วยข้อมูลจริงจาก Supabase ใน Phase 3

---

## 🎨 การออกแบบ UI/UX

### สี
- **สีหลัก**: น้ำเงิน (blue-600)
- **รายรับ**: เขียว (emerald-600)
- **รายจ่าย**: แดง (rose-600)
- **เตือน**: ส้ม (orange-600)

### Animation
- การ์ดทุกใบมีแอนิเมชั่นเข้าหน้า (fade + scale)
- Hover มีเงา
- Modal เปิดแบบ spring

### Responsive
- **Desktop** (>768px): แสดง Sidebar ด้านซ้าย
- **Mobile/Tablet** (<768px): แสดง Top Navigation แทน
- ทุกการ์ดปรับขนาดอัตโนมัติ

---

## 🔧 ปัญหาที่พบบ่อย

### 1. หน้าจอว่างเปล่า
**สาเหตุ:** เซิร์ฟเวอร์ยังไม่รัน
**แก้ไข:** รัน `npm run dev`

### 2. ข้อมูลไม่แสดง
**สาเหตุ:** Mock data มีปัญหา
**แก้ไข:** ตรวจสอบ console ใน DevTools (F12)

### 3. ปุ่ม "เพิ่ม" ไม่ทำงาน
**คำตอบ:** ยังไม่ได้ทำ Modal ยังอยู่ใน Phase 2

### 4. ปฏิทินไม่แสดง
**คำตอบ:** ยังเป็น placeholder ยังอยู่ใน Phase 2

---

## 🚀 Phase ถัดไป

### Phase 2: Modal & Forms (กำลังพัฒนา)
- ฟอร์มเพิ่ม/แก้ไข Transaction
- ฟอร์มเพิ่ม/แก้ไข Schedule
- ฟอร์มเพิ่ม/แก้ไข Student
- Modal รายละเอียดต่างๆ

### Phase 3: Supabase Integration
- เชื่อมต่อฐานข้อมูลจริง
- Real-time updates
- อัพโหลดรูปโปรไฟล์

### Phase 4: Advanced Features
- Export Excel/PDF
- แจ้งเตือน SMS/Email
- Analytics แบบละเอียด

---

## 📞 การติดต่อ & การสนับสนุน

หากพบปัญหาหรือต้องการเพิ่มฟีเจอร์:
1. ดูไฟล์ `DEVELOPMENT.md` สำหรับคนพัฒนา
2. ดู `README.md` สำหรับข้อมูลทั่วไป
3. ตรวจสอบ Issues ใน GitHub (ถ้ามี)

---

## 💡 เคล็ดลับ

1. **ใช้ Dark Mode**: เบราว์เซอร์จะปรับอัตโนมัติตามระบบ
2. **ดูรายละเอียด**: Hover เหนือการ์ดต่างๆ จะมีเอฟเฟกต์
3. **กรองรายการ**: ใช้ตัวกรองหลายอันพร้อมกันได้
4. **เลื่อนดู**: Schedule Cards เลื่อนด้วยลูกศรซ้าย-ขวา

---

## 🎓 สำหรับนักเรียน/ครู

### ครูสามารถ:
- ✅ ดูภาพรวมการเงินห้อง
- ✅ ติดตามนักเรียนที่ค้างชำระ
- ✅ บันทึกรายรับรายจ่าย
- 🔜 เพิ่มกำหนดการใหม่
- 🔜 ส่งแจ้งเตือนนักเรียน

### นักเรียนสามารถ:
- 🔜 ดูสถานะการชำระของตัวเอง
- 🔜 ดูประวัติการชำระ
- 🔜 พิมพ์ใบเสร็จ

---

## 📈 สถิติการใช้งาน

ระบบจะบันทึก:
- จำนวนธุรกรรมทั้งหมด
- ยอดเงินสูงสุด/ต่ำสุด
- กำหนดการที่เก็บได้มากที่สุด
- นักเรียนที่ชำระตรงเวลา

(ฟีเจอร์นี้จะมาใน Phase 4)

---

ขอให้ใช้งานอย่างมีประสิทธิภาพ! 🎉
