interface:
  display_name: "Git PR Helper"
  short_description: "Prepare commits and PRs"
  default_prompt: "Use $git-pr-helper to summarize and prepare repository changes."
policy:
  allow_implicit_invocation: true
