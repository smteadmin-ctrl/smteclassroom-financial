import { LoadingScreen } from "@/components/layout/LoadingScreen";

export default function Loading() {
  // This is automatically shown during route segment loads and client navigations
  return <LoadingScreen />;
}
